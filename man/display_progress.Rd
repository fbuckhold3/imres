% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/function_assessment.R
\name{display_progress}
\alias{display_progress}
\title{Display Progress Bars in a Shiny UI}
\usage{
display_progress(progress_data)
}
\arguments{
\item{progress_data}{A data frame with columns:
\describe{
  \item{Level}{Resident level (character).}
  \item{Total}{Numeric total count of evaluations.}
  \item{Progress}{Numeric percent (0--100) of completion.}
}}
}
\value{
A \code{\link[shiny]{tagList}} of \code{div}s suitable for rendering
  in a Shiny UI.
}
\description{
Creates a list of Bootstrap progress bars for each resident level,
  given a data frame with columns \code{Level}, \code{Total}, and \code{Progress}.
}
\examples{
\dontrun{
  ui <- fluidPage(
    uiOutput("progress_bars")
  )

  server <- function(input, output, session) {
    progress_df <- data.frame(Level = c("PGY1","PGY2"), Total = c(10, 15), Progress = c(40, 60))
    output$progress_bars <- renderUI({
      display_progress(progress_df)
    })
  }
  shinyApp(ui, server)
}

}
