% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/function_assessment.R
\name{prep_obs_table}
\alias{prep_obs_table}
\title{Prepare and Display Observation Table}
\usage{
prep_obs_table(data, dict, resident, selected_label, caption = NULL)
}
\arguments{
\item{data}{A data frame of observations containing columns such as \code{name},
\code{ip_obs_type}, \code{Date}, etc.}

\item{dict}{A data frame data dictionary with columns \code{field_name} and
\code{field_label}.}

\item{resident}{A character string for the resident name.}

\item{selected_label}{The chosen observation label to filter on. Must match the
strings in \code{ip_obs_type}.}

\item{caption}{An optional caption for the output table. Defaults to
\code{"Details for: {selected_label}"} if \code{NULL}.}
}
\value{
A \code{\link[DT]{datatable}} object, styled by \code{\link{create_styled_dt}}.
}
\description{
A helper function that filters data for a specified resident and
  observation type label, drops empty/unwanted columns, renames columns based on
  the data dictionary, then returns a styled \code{\link[DT]{datatable}}.
}
\examples{
\dontrun{
  # If 'my_dict' is your REDCap dictionary and 'obs_data' is your main data:
  output$obs_table <- DT::renderDT({
    prep_obs_table(obs_data, my_dict, "John Doe", "Physical Exam")
  })
}

}
