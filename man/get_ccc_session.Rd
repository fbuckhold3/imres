% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils_general_data_wrangle.R,
%   R/utils_redcap_submit.R
\name{get_ccc_session}
\alias{get_ccc_session}
\title{Get CCC Session Value}
\usage{
get_ccc_session(period_value)

get_ccc_session(period_value)
}
\arguments{
\item{period_value}{A character string representing the period (e.g., "Mid Intern", "Interim Review").}
}
\value{
An integer corresponding to the `ccc_session` value, or NA if no match is found.

An integer corresponding to the `ccc_session` value.
}
\description{
Returns the session value (`ccc_session`) for a specific period.

Utility functions to interact with REDCap API.
These functions support pulling data, calculating instance numbers, and submitting data to REDCap.

Get the `ccc_session` value based on the period.
}
\details{
This file includes the following key functions:

- **`fetch_record_id()`**: Retrieve the `record_id` for a given resident name.
- **`generate_new_instance()`**: Get the next instance for a specific instrument and record.
- **`submit_to_redcap()`**: Submit a new record to REDCap.
- **`fetch_existing_data()`**: Pull existing data for a specific period, record, and instrument.
- **`validate_inputs()`**: Validate that user inputs are within the required range.
- **`update_inputs()`**: Update Shiny UI inputs with existing REDCap data.

Get CCC Session Value
}
\examples{
\dontrun{
 session <- get_ccc_session("Mid Intern")
 print(session) # Returns 1
}
}
